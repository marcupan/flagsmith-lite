.idea

node_modules

# Build output — in all packages
**/dist/

# Turborepo cache
.turbo

.DS_Store
.env
.env.dev
.env.local
.env.production

# Legacy: was `docs/*/` which blocked all subdirs.
# Now explicitly allow all docs subdirectories.

# Local-only study/learning materials (not pushed to GitHub)
docs/*-study/
