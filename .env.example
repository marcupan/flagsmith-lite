# docker-compose overrides — copy to .env and fill in values before running

# n8n automation (required — no defaults to avoid accidental admin/admin exposure)
# Generate: openssl rand -hex 16
N8N_USER=your-n8n-username
N8N_PASSWORD=your-n8n-password
